
services:

  django:
    build:
      context: .
      dockerfile: docker/exfilserver/Dockerfile
    container_name: exfilserver
    ports:
      - "2500:2500"
    environment:
      PYTHONPATH: "/usr/src/app"